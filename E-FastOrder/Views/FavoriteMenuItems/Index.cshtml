@model IEnumerable<E_FastOrder.Models.FavoriteMenuItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MenuItem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MenuItem.MenuItemId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FavoriteMenuItemId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FavoriteMenuItemId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FavoriteMenuItemId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
